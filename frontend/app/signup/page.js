'use client'
// Similar structure to /login but with full_name + confirm password fields
// Use signUp() from lib/auth, same error handling pattern
// Redirect to /login with toast "Check your email to confirm"
